import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containers: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  title: {
    fontSize: 20,
    fontWeight: "bold",
    color: "red",
  },
  image: {
    width: 200,
    height: 200,
  },
});
